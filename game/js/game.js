"use strict";
/**
 * Initializes a new game state
 * @returns {GameState} A new game state object.
 */
const init_game_state = () => {
    return {};
}

/**
 * Iterates an active game state by an iteration
 * @param {GameState} game_state - The unput game state to be mutated.
 * @param {Number} dt - The time since the last update.
 */
const iterate_game_state = (game_state, dt) => {

}
